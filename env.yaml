name: cv_proj
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11.4
  - pytorch=2.4
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - moviepy
  - numpy
  - tensorboard
  - mypy
  - pip

  - pip:
      - pytorch-ignite
      - opencv-python
      - torch-snippets
